<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <!-- Device-to-device gallery transfer: local-socket payload + QR scan handshake. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <!-- Bluetooth transfer & pairing (bonded devices + discovery for in-app pairing). -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <!-- neverForLocation: we only discover devices to pair, never to derive physical location. On
         API 31+ this lets discovery run without system Location being enabled (the Pixels were
         spinning forever because BLUETOOTH_SCAN implied location and Location was off). -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <!-- Pre-31 Bluetooth discovery returns no results without location permission. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" android:maxSdkVersion="30" />
    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />
    <!-- Recordings live in public storage (/sdcard/Documents/FFTT04M) so they survive uninstall.
         API ≤29 uses WRITE_EXTERNAL_STORAGE; API 30+ uses MANAGE_EXTERNAL_STORAGE ("All files access"). -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <!-- Background transfer + per-file notifications. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Hands-free background/locked cough auto-capture (foreground microphone service + CPU wake lock). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:requestLegacyExternalStorage="true"
        android:theme="@style/Theme.FFTT04M">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/title_listening"
            android:screenOrientation="fullSensor">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".GalleryActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:label="@string/title_gallery"
            android:screenOrientation="fullSensor">
            <!-- Receive a shared gallery bundle (a .zip) from Quick Share / Files / etc.:
                 tapping it offers FFTT, which imports it. The handler validates the contents,
                 so non-FFTT zips are politely rejected. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:mimeType="application/zip" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ViewerActivity"
            android:exported="false"
            android:label="@string/title_analysis"
            android:screenOrientation="fullSensor" />

        <activity
            android:name=".WaveletActivity"
            android:exported="false"
            android:label="@string/title_wavelet"
            android:screenOrientation="fullSensor" />

        <activity
            android:name=".cough.CoughAnalysisActivity"
            android:exported="false"
            android:label="Cough Analysis"
            android:screenOrientation="fullSensor" />

        <activity
            android:name=".cough.ClipCloudActivity"
            android:exported="false"
            android:label="Clip Cloud"
            android:screenOrientation="fullSensor" />

        <activity
            android:name=".cough.CoughCaptureActivity"
            android:exported="false"
            android:label="Cough Auto-Capture"
            android:screenOrientation="fullSensor" />

        <activity
            android:name=".LatencyActivity"
            android:exported="false"
            android:screenOrientation="fullSensor" />

        <activity
            android:name=".ManualActivity"
            android:exported="false"
            android:label="User Manual"
            android:screenOrientation="fullSensor" />

        <activity
            android:name=".ExportActivity"
            android:exported="false"
            android:label="Send gallery"
            android:screenOrientation="fullSensor" />

        <activity
            android:name=".BtSendActivity"
            android:exported="false"
            android:label="Send via Bluetooth"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboardHidden"
            android:screenOrientation="fullSensor" />

        <activity
            android:name=".BluetoothPairingActivity"
            android:exported="false"
            android:label="Pair Bluetooth Device"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboardHidden"
            android:screenOrientation="fullSensor" />

        <service
            android:name=".TransferService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice|dataSync" />

        <service
            android:name=".CoughCaptureService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <!-- For sharing gallery bundles to other apps/devices via the system share sheet. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>